package com.jh.stock.domain;

public class MyStockTest {
//    @Test
//    public void return_ZERO_when_quoteList_is_null_or_empty_test() {
//        MyStock emptyMyStock = MyStock.builder().build();
//        MyStock emptyQuoteMyStock = MyStock.builder().quoteList(Collections.emptyList()).build();
//
//        assertThat(emptyMyStock.latestClosingPrice()).isEqualTo(BigDecimal.ZERO);
//        assertThat(emptyQuoteMyStock.latestClosingPrice()).isEqualTo(BigDecimal.ZERO);
//    }
}
